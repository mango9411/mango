spring:
  application:
    name: spring-boot-service
  cloud:
    config:
      enabled: true            #是否连接配置中心
      uri: http://localhost:7777/
      profile: dev
      name: serviceCopyApplication

